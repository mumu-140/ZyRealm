# P1A.1 Routing Inspector / Explain Routing Engine — Merge & Deployment Verification Record

> Status: **MERGED + MERGED-TREE VERIFIED + DEPLOYED + POST-DEPLOY VERIFIED**.
> This is the authoritative closure record for P1A.1. It does not start P1A.2.

## Integration evidence

- Branch: `feat/p1a-routing-inspector`, final head `4e64ece6bef5667b165e40b4d71554b4e2af19fb`.
- Final branch CI: run `35004111740` — governance, backend `go vet ./...` + `go test -buildvcs=false ./...`, frontend lint + tests + production build, all success on the exact head.
- PR: `#27 feat(routing): add historical Routing Inspector` (base `main`), mergeable `CLEAN`, all required checks success, no unresolved review threads, no changes requested.
- Merge: **squash merge** (repo convention) at `2026-09-15T18:12:54Z`.
- Squash merge commit on `main`: `1f4b856b350c3941000e1ca9af12c7ed42f67622`.
- Merged-tree CI: run `35006072380` on `main@1f4b856b…` — governance, backend, frontend all success.

## Review conclusions (fresh source audit, 25 files, +1944/−37)

- Data-plane neutrality: `Iterator.RecordDecision` only appends request-local observational events; `Attempts()` materializes a detached clone at the persistence boundary; candidate ordering/branching unchanged (`runtimeOrderedCandidatesWithDecisions` preserves the exact prior eligibility semantics); `availability.CandidateInfo` is a pre-existing read-only snapshot API; no lease, fairness, reservation, retry or budget mutation on the capture path (`runtime_candidate_decision_test.go` asserts tracing does not change executable candidates).
- Historical truth: `internal/routinginspect` builds explanations only from persisted `RelayLog` facts (imports limited to `internal/model` + stdlib, enforced by `boundary_test.go` which forbids relay/availability/db/op/server imports).
- Privacy boundary: `RoutingDecisionEvent.Detail` is stripped in the Explain projection (`sanitizeDecisionEvent`); new capability-negative events persist no free text; typed contract is stage/outcome/reason/identity/model/protocol/expiry only.
- Statistics isolation: decision-only envelope creates no site-model traffic (`op/stats_site_model_decision_test.go`) and does not affect final-route resolution or final model (`relay/routing_inspector_metrics_test.go`).
- Frontend zero-noise: Routing Inspector defaults collapsed; `useRoutingExplanation(expanded ? selectedLogID : null)` with `enabled` gating — no `/api/v1/log/:id/routing` query while collapsed; `refetchOnWindowFocus: false`. Active Requests untouched.
- Known deferred (recorded, not implemented): dispatch/failover stage events and `selected`/`attempted` outcomes exist in the typed contract but no capture point emits them yet (rejection-path stages only in this slice).

## Deployment

- Tag: `v0.11.0-mumu.6` on `1f4b856b350c3941000e1ca9af12c7ed42f67622`; Release run `35007860714` success; GHCR image `ghcr.io/mumu-140/zyrealm:v0.11.0-mumu.6`, image/digest `sha256:d498f89e10955da51ef4218b44bc3bbc03581a3f3507ad40576f92a683ad6060`; OCI labels verified (`revision` = merged commit, `source-tree` = `4e0b3e7a4c3ce9953f17c03ba1134a0968507d52`).
- Pre-cutover verified snapshots: `pre-v0.11.0-mumu.6-cutover-20260915T183007Z` (sha256 `4c24137e…`, quick_check ok) and `pre-v0.11.0-mumu.6-cutover-20260915T205123Z` (sha256 `2404e4d1…`, quick_check ok).
- Candidate validation: isolated container `octopus-candidate-v0.11.0-mumu.6(-r2)` on `127.0.0.1:35277` with verified data copy; full smoke green before production switch.
- Final cutover: `cutover-v0.11.0-mumu.6-20260915T210943Z` — detached background task with `set -Eeuo pipefail`, `flock`, snapshot verification, health waits, automatic rollback trap; status **COMPLETE**, no rollback in the final run.
- Rollback point: prebuilt container `zyrealm-rollback-v0.11.0-mumu.5` (image `ghcr.io/mumu-140/zyrealm:v0.11.0-mumu.5`, created/exited state) + verified snapshot above.

### Failed cutover attempts (recorded, auto-rolled back, no production impact beyond a restart blip)

1. `cutover-v0.11.0-mumu.6-20260915T140500Z` — ROLLED BACK. Smoke failures (`responses_api`, `chat_nonstream_final` client-side 60 s timeouts; WS round-1 missed its 45 s window). Root cause: third-party upstream channels hung ~60 s each during the smoke window while the smoke client timeout (60 s) was tighter than the server's own failover budget; mumu.6 process itself healthy (failover completing, no restart, quick_check ok). Fix: smoke client timeouts raised (HTTP 120–180 s, WS window 150 s) so the server can finish its own failover.
2. `cutover-v0.11.0-mumu.6-20260915T205418Z` — ROLLED BACK. Smoke script defect (`http_req` missing `url` binding after a cleanup edit; syntax check cannot catch NameError). Fix: repaired and runtime-verified the script before retry; candidate smoke re-run to 28/28 before touching production.
3. `cutover-v0.11.0-mumu.6-20260915T210943Z` — **COMPLETE**, production smoke 28/28.

## Post-deploy verification (production, `fwq57ys`)

- Container `zyrealm`: image `ghcr.io/mumu-140/zyrealm:v0.11.0-mumu.6`, id `428e51607b31c542f55014ce5dc27e56aa7373beda87d55e27f8b2df636ab94a`, host network, `unless-stopped`, restarts=0, mount `/home/yangs/API/ZyRealm-data/data -> /app/data`.
- Startup banner: `v0.11.0-mumu.6` / commit `1f4b856`.
- HTTP: `http://127.0.0.1:35276/` 200, `http://222.28.118.57:35276/` 200; auth boundary 401 without key.
- Logs: no own-process panic/FATAL; zero own cooldown/circuit/suspect pollution markers (the only grep hit is an upstream error body reporting the upstream's own cooldown).
- Routing smoke on production: explanation endpoint `GET /api/v1/log/:id/routing` 200 with typed contract (`completeness=complete` for new traffic), legacy log degrades as `legacy_partial`, no `Detail`/prompt/secret fields in responses, 401 without admin JWT, typed decisions observed on real traffic.
- Relay smoke: non-stream chat, stream chat (with `[DONE]`), `/v1/responses`, WS `response.create` rounds (created → deltas → completed), Active Requests list/interrupt, Logs list — all PASS (`smoke-result.json` in the cutover run dir).

## Changed-file scope

- DB migration: **no** (decision events ride inside existing serialized `Attempts`).
- Deployment script change: **no** (existing tag → Release → GHCR → snapshot → candidate → cutover process used as-is).
- Production config change: **no** beyond the compose image bump that is part of the existing cutover process.

## Roadmap

- P1A.1 Routing Inspector / Explain Routing Engine → **CLOSED** (merged, merged-tree verified, deployed, post-deploy verified).
- P1A.2 Stateless Route Simulator → **DEFERRED / NEXT DESIGN AUDIT** (not started; requires its own source audit and design).
- Dispatch/failover stage capture → follow-up slice within the existing typed contract.
